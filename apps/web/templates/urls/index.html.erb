<h2>All urls</h2>

<% if urls.any? %>
  <div id="urls">
    <% urls.each do |url| %>
      <div class="url">
        <h2><%= url.title %></h2>
        <p><%= url.address %></p>
      </div>
    <% end %>
  </div>
<% else %>
  <p class="placeholder">There are no urls yet.</p>
<% end %>
